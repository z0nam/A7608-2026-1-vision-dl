# Chapter 3 essentials
# Used by `chap03/python_3장.ipynb`, `chap03/colab_3장.ipynb`,
# and the helper scripts under `chap03/code/`.
numpy==1.26.4
pandas==2.2.3
matplotlib==3.9.2
seaborn==0.13.2
scikit-learn==1.5.2
tensorflow==2.16.2

# Other chapters / repo-wide
# OpenCV:
# - Pin to a NumPy 1.26-compatible build so it does not force NumPy 2.x and
#   break TensorFlow-based chapters in the shared environment.
opencv-python==4.10.0.84
#
# PyTorch stack:
# - `torch==1.13.1` works with Python 3.10 in this repo.
# - `torchtext==0.14.1` does NOT provide `torchtext.legacy`.
# - Chapter 7 notebooks still use the legacy `Field`/`BucketIterator` API and
#   therefore need a separate older environment (for example Python 3.7/3.8
#   with a matching legacy torchtext stack) or a notebook refactor.
# - `torchvision==0.14.1` matches the same torch release for Chapters 5, 7, 8, 12, 13.
torch==1.13.1
torchvision==0.14.1
torchtext==0.14.1

# Notebook rendering / seminar workflow
ipykernel==7.2.0
ipywidgets==8.1.8
jupyter==1.1.1
jupyterlab==4.5.6
nbconvert==7.17.0
nbformat==5.10.4
notebook==7.5.5

# Optional: only needed when running notebooks inside Google Colab.
# google-colab

# Chapter 3 OCR tooling uses external CLI tools, not pip packages:
# - `tesseract`
# - `sips` (macOS)
